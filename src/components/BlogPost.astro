---
const { url, title } = Astro.props;
---

<li>
  <a href={url}>{title}</a>
</li>

<style>
  li {
    list-style-type: none; /* 去掉小圆点 */
  }
  li::before {
    content: '>'; /* 将小圆点改成 '>' 符号 */
    color: #0099cc; /* 绿蓝色 */
    margin-right: 5px; /* 调整符号与文本之间的间距 */
  }
  a {
    color: #0099cc; /* 绿蓝色 */
    text-decoration: none; /* 去除默认下划线 */
  }
  a:hover {
    color: #ffcc00; /* 鼠标移上去时显示高亮 */
  }
</style>
